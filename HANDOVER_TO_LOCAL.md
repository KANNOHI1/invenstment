# ローカル環境への引き継ぎ書（2026-08-28作成／**2026-09-07 更新**）

**2026-09-08、ユーザーが移行の実行を決定した。** この書類がその手順書。
2026-09-07に8日分の変化を反映し、2026-09-08に最新の判断を追記している。

## ★ ローカルのClaude Codeに最初に貼るテキスト（これだけでよい）

```
このプロジェクトをクラウドのClaude Codeから引き継ぐ。
まず HANDOVER_TO_LOCAL.md を全部読んで、そこに書かれた順番どおりに進めてほしい。
作業ブランチは claude/project-status-review-gretkk（mainではない）。
```

以降はこの書類の「1. 最初にやること」から順に進める。

## ★ 移行の残り手順（2026-09-08 時点。ユーザーに聞かずここを見て進めること）

**ユーザーは非エンジニアで、クラウド側セッションとの伝言役になっている。
判断を求めず、この順番どおり自分で進めること。** 決定済みの事項:

- **レポートはセッションに届ける。メール配信は要件を満たさないので採用しない**（下記★を読むこと）
- **クラウドの Routine はまだ稼働している。** 止めるのはクラウド側セッションの仕事
- `setup_task_scheduler.ps1` の実行可否は下記の順番で自律的に判断してよい

| # | やること | 次へ進む条件 |
|---|---|---|
| 1 | `git pull --rebase` | 認証情報の隔離修正（`f0e351e`）が入ること |
| 2 | **セッション型で実装済み（2026-09-10 00:26 初回実行が成功、下記★）** | 済 |
| 3 | 平日 JST 7:00 に登録 | 済（Task Scheduler `InvestmentMorningPatrol`、action は `cmd.exe /k scriptsun_patrol.cmd`） |
| 4 | 9/10 07:00 の自動実行で **同じセッション「Morning patrol」に追記される**ことを確認 | スマホの同スレッド末尾に新しい朝のターンが足されていること |
| 5 | 4 が通ったらクラウド側が Routine を無効化。ローカルは何もしない | — |

**試走で失敗したら、ユーザーに投げ返さず自分で原因を切り分けて直すこと。**
よくある詰まり: 1Password の認証ダイアログ（手元での承認が要る）、
`claude.cmd` のパス、`GMAIL_APP_PASSWORD` が env に無い、`claude -p` のタイムアウト。

**次セッションの本題**は §2-2 の SEC EDGAR 取得（`fetch_filings.mjs` の SEC 版復元）。
2026-09-08 に到達性は確認済み（200）。宿題1〜9のうち 1〜8 はこれで解ける。

**このファイルはローカルのClaude Codeが最初に読むもの。** クラウド環境（claude.ai/code）から
ローカルへ**完全移植**するために必要な情報を、漏れなくここに集約してある。

## 前提: ローカル一本化。クラウド側は停止する。

ユーザーの明示的な要望: **「全部ローカル一本化でいい。二つに割れると管理できない」**
したがって以下は**分岐を残さず、クラウド側の停止まで含めて完了させること**。
「クラウドに残してもよい」という選択肢は提示しない。

---

## 0. 30秒で理解する現状

- **何をしているか**: 米国株の投資分析。ユーザーは顧客で、こちらは分析を出すエージェント。
- **目的関数**: 総資産の非対称な大幅リターン（3倍以上）。個別トレードの勝敗ではない。
- **今の成果物**: 毎朝の「相場観レポート」（マクロ座標→セクターローテ→主役→保有への言及）。
- **売買は提案しない**（2026-08-22の方針転換）。保有はユーザーが決め、変更の連絡があったときだけ記録を更新する。
- **移植する理由**: クラウドからは企業の一次データ（SEC提出書類）に到達できず、企業研究の粒度が上がらない。詳細は `research/data_source_limits.md`。

---

## 1. 最初にやること（順番どおり）

```bash
git clone https://github.com/KANNOHI1/invenstment.git
cd invenstment
git checkout claude/project-status-review-gretkk   # 作業ブランチ。mainではない
node --version                                      # 20以上が必要
```

**読む順番:**
1. `CLAUDE.md` — 恒久ルール。毎セッション自動で読まれる。**まずこれ。**
2. `STATUS.md` の冒頭「現在地」ブロック — 保有・資金・未解決
3. `watchlist/rotation_state.md` — 前回の相場観（差分の基準点）
4. `watchlist/thesis_register.md` — 仮説と反証条件、事故記録
5. `research/data_source_limits.md` — なぜ移植したか
6. このファイルの残り

**動作確認:**
```bash
node scripts/rotation_check.mjs   # 象限判定・セクター序列・主役
node scripts/patrol_check.mjs     # 保有の軌跡・仮説の反証チェック
node scripts/scan_rank.mjs        # 保有外の候補スキャン
```
（既存のJSONを読むだけなので、株価を取らなくても動く）

---

## 1-B. クラウド側を停止する（一本化の必須作業）

**ローカルで動作確認が取れてから実施すること。** 順番を守る。

### (1) 定期巡回のRoutineを止める
claude.ai の設定 → Routines →「**相場観レポート（定期巡回）**」
（trigger_id: `trig_01TUF9eRUquZFc1QAicAT2UK`、cron `0 23 * * 1-5` ＝平日 23:00 UTC ＝ 翌 08:00 JST）を
**削除または無効化**する。

**これを止めないと、クラウド側からもレポートが届き続け、ローカルの出力と二重になる。**

**2026-09-04の状況変化**: 承認が通らなかった `update_trigger` が使えるようになり、
トリガー文面は相場観 v1.0仕様へ差し替え済み（旧名称「投資ポートフォリオ定期巡回」から改名）。
**したがって停止もエージェント側から実行できる可能性が高い**が、
Routineの削除は取り消せないので**必ずユーザーに確認してから**行う。
MCP接続が落ちている場合はユーザーが画面から止める。

### (2) GitHub Actionsのワークフローを止める
`.github/workflows/price-snapshot.yml` には定時実行（cron）が入っている。
ローカルで株価を取るなら不要なので、**cronのスケジュールを削除**する（ファイルごと消してもよい）。
残すと、ローカルの取得とActionsの取得が競合してgitのコンフリクトを起こす。

同様に `earnings-monitor.yml` と `dashboard-pages.yml` も、ローカル運用で使わないなら止める。

### (3) 一本化したことをCLAUDE.mdに記録
`CLAUDE.md` 冒頭の「定期巡回について」の項は**クラウドのトリガーについての記述**なので、
**丸ごと削除**し、ローカル運用（cron/launchd等でどう起動するか）の記述に置き換える。
※2026-09-04に「旧仕様なので無視せよ」という回避策の記述は撤去済み。現在は差し替え済みの事実だけが書いてある。

---

## 2. ローカルで最初に直すべきこと（クラウドではできなかった）

### 2-1. 株価取得をローカル実行に切り替える（最初にやる）

**クラウドではegress遮断で株価を取れず、GitHub Actions経由という回りくどい手順を使っていた。**
ローカルなら直接取れる。まず確認:

```bash
node scripts/fetch_prices.mjs
```

成功すれば `watchlist/latest_prices.json`・`scan_prices.json`・`rotation_prices.json` が更新される。

**成功したら以下を実施（一本化に必須）:**
- `CLAUDE.md` の「**株価はGitHub Actions経由でのみ取得する**」「**`fetch_prices.mjs`をローカルで実行しない**」を**削除し、ローカル直接実行に書き換える**。これはクラウド固有の制約への対処で、ローカルでは逆に足枷になる。
- `watchlist/report_template_rotation.md` の生成手順①も同様に書き換える。
- `watchlist/.price-refresh-trigger` は役目を終える（削除してよい）。
- `.github/workflows/price-snapshot.yml` を停止（上記1-B(2)）。

**注意**: `fetch_prices.mjs` には「取得が全滅したら既存ファイルを上書きしない」ガードが入っている。
これはローカルでも有用なので**残すこと**（2026-08-21に良好なスナップショットを空データで潰した事故の対策）。

### 2-2. SEC EDGARからの一次データ取得を作る（最優先の宿題）

**クラウドではSECがIPを403で遮断していて不可能だった。ローカル（家庭用IP）なら開ける。**

まず到達性を確認:
```bash
curl -s -o /dev/null -w "%{http_code}\n" \
  -A "your-name your-email@example.com" \
  "https://data.sec.gov/submissions/CIK0001878848.json"
```
200が返れば使える。**SECはUser-Agentに「名前＋連絡先メール」を要求する**（無いと403）。

`scripts/fetch_filings.mjs` に途中まで書いた実装がある（Yahoo版に書き換わっているので、
SEC版はgit履歴の `決算の一次データをSEC EDGARから取得する仕組みを追加` コミットを参照）。
`watchlist/cik.json` にCIK番号と社名の照合データがある。**社名照合は必ず残すこと**——
番号を1桁間違えると別会社のデータを自社のものとして使う事故になる。

取りたいXBRL概念:
`dei:EntityCommonStockSharesOutstanding`（表紙の株数）、`us-gaap:Revenues`、
`us-gaap:NetIncomeLoss`、`us-gaap:AssetImpairmentCharges`、
`us-gaap:CashAndCashEquivalentsAtCarryingValue`、
`us-gaap:WeightedAverageNumberOfDilutedSharesOutstanding`

### 2-3. 銘柄ごとの調査ファイルを作る（未着手）

**現状、巡回のたびにゼロから調べ直していて知識が蓄積しない。** これが「企業研究の粒度が浅い」
の直接原因の一つ。`research/companies/{TICKER}.md` を作り、検証済みの事実＋出所＋日付を
積み上げる運用にすること。

---

## 3. ユーザーについて（聞き直さないこと）

- **証券会社: SBI証券。時間外・プレマーケットは未対応**（2026年内に対応予定）。
  **執行は22:30 JST〜の通常取引のみ。** 指値は事前に置けば寄りで執行される。
- **予備費として日本円トータル¥100万を死守。** 現在は口座外に確保済み。
- NISA成長投資枠は2026年分の残が¥19,965＝実質使い切り。
  NISAは損失通算ができないため、**損失が出うる建玉は特定口座に置くのが原則**。
- ユーザーは**非エンジニア**。技術的な説明は噛み砕く。専門用語を並べない。
- **モバイルで読む**ため、表は最大3列。横長テーブル禁止。
- 日本語で書く。

### 保有（`watchlist/holdings.json` が機械可読ソース。株価は2026-09-04終値）

| 銘柄 | 株数・取得単価 | 現値 |
|---|---|---|
| IREN | 112 @ $49.28（NISA） | $44.68 |
| POWL | 15 @ $207.56（NISA7＋特定8） | $181.17 |
| SIMO | 10 @ $254.86（NISA） | $256.21 |
| MOD | 10 @ $201.12（NISA6＋特定4） | $194.66 |
| MU | 2 @ $682.00（NISA） | $1,016.59 |

評価額 $14,264。現金 $4,335.01＋¥34,780（2026-08-21 22:38 アプリ確認）。参考為替 159.34。
予備費¥1,000,000は口座外に確保済み。

**保有の一次ソースはユーザーの証券アプリ画面。** こちらの記録と食い違ったらアプリが正。
**ユーザーから変更の連絡があったときだけ更新する。こちらから売買を提案しない。**

### 売買の方針（2026-09-01〜03にユーザーが決定。これが最新）

- **価格による損失線・撤退線は引かない。** 9/1に全廃した。
- **売却の理由は2つだけ。** ①**事業の論理が壊れたとき** ②**象限不整合**——
  事業が健全でも、資金が入らない層にいるなら降ろす。
- 保有の耐久性ランキング（2026-09-03、`watchlist/handan_2026-09-03.md`）:
  **POWL=A**（有利子負債ゼロ、現金$634M、FCF TTM +$244M）／**MU=A**（2028年度まで満期なし）／
  **MOD=B**（負債$521.7Mだが2027年末までの満期は$8.3Mのみ）／**SIMO=B**（転換社債$1.15Bで現金確保）／
  **IREN=C**（下記）
- **9/3の判断: IRENのみ降ろす。他4銘柄は金利ピークまで保有。**
  IRENは2026-08-28にBlue Owl／PIMCOから**金利9%・満期2.5年のシニア担保付債務$24億**を調達
  （タームローン$12億＋ノート$12億、用途はBlackwell Ultra GPU購入。8-Kで一次確認済み）。
  既存の有利子負債$7,593Mへの上乗せで、**金利上昇局面で9%・2.5年は最も不利な条件**。
  **執行はまだ行われていない。ユーザーが決める。**
- **待機資金（$4,335）の投下条件**（どれか1つで発火）:
  ①FOMCが利上げ見送り＋文言中立化 ②原油が20日・13週ともマイナス転換
  ③公益（XLU）が20日で上位3位 ④**時限 2026-12-31**

---

## 4. 毎朝の巡回をローカルで動かす

**クラウドのRoutineは停止し（1-B参照）、同じ定期巡回をローカルで自動実行する。**

**ユーザーは他のプロジェクトで、ローカルのClaude Codeを定期自動実行させる運用を既に複数持っている。**
このプロジェクトも同じ方式に合わせること。具体的な仕組み（スケジューラの種類・起動方法）は
ユーザーの既存セッションに倣うのが最短なので、**引き継ぎ直後に「他のセッションと同じ方式で
毎朝の巡回を自動化して」と確認し、同じ形で組む**。

- **発火時刻: JST 7:00（＝22:00 UTC）。ユーザーの指定で確定。変更しないこと。**
  米国市場の引けは夏時間20:00 UTC（JST 5:00）／冬時間21:00 UTC（JST 6:00）。
  **JST 7:00なら年間を通じて引け後になる**ため、サマータイムの切り替えで設定を変える必要がない。
  クラウドでは23:05 UTC（JST 8:05）に発火していたので、移植で1時間強の前倒しになる。
- **★レポートは「続きの会話ができるセッション」に届ける。メール配信は不採用（2026-09-08 確定）。実装完了（2026-09-10）。**

  **要件はレポートの配達ではなく、会話が始まっていること。** 菅野さんは朝レポートを読んだ直後に
  「MUをもっと調べて」とその場で続けたい。メールは一方通行なので満たさない。

  **採用した仕組み（他プロジェクトと同じ Remote Control 経由）**
  - `~/.claude/settings.json` の `remoteControlAtStartup: true` により、PC で起動した対話セッションは
    全部 Remote Control に載り、claude.ai/code とスマホアプリから開いて返信できる。菅野さんが
    「他のセッション」で使っていたのはこれ（デスクトップアプリのスケジュール機能ではない）
  - Task Scheduler `InvestmentMorningPatrol`（平日 07:00）→ `cmd.exe /k scriptsun_patrol.cmd`
    → `claude --resume <固定ID> --dangerously-skip-permissions "<起動指示>"`。
    **毎朝、同じ 1 セッション（固定 ID `36358243-ef8c-4c8b-ba08-30112934c274`）に追記する**。
    初回だけ `--session-id` で作成（2026-09-10 00:26 実施済み）
  - 起動指示は ASCII 1 文で「`watchlist/trigger_prompt_v1.md` の区切り線の間を実行せよ」とだけ渡す（正はそのファイル 1 箇所）
  - 前提: PC が起動していること（ClaudeHarnessWeekly と同じ）。ExecutionTimeLimit は無制限（セッションを日中開いたままにするため）

  **試して駄目だった経路（再試行しないこと）**
  - **デスクトップアプリの scheduled task**: 既定の許可モードで走り最初の Bash で承認待ちになる。
    プロジェクト `.claude/settings.json` の `defaultMode: bypassPermissions` も `permissions.allow` も効かなかった。
    さらに毎回新規セッションになる仕様で、要件（同じセッションに追記）と合わない。3 本とも削除済み
  - **`claude -p`＋Gmail SMTP**（`scripts/patrol_daily.py`）: 一方通行で差し戻し。ファイルは残しているが主経路ではない
  - **PowerShell `Start-Process` 経由の起動**（旧 `run_patrol.ps1`）: claude が会話記録（`.jsonl`）を残さず、翌朝 `--resume` が
    「No conversation found」で失敗した。Task Scheduler の action から `cmd.exe` で直接起動すれば記録が残る
  - **Claude Code のセッション内のツールから対話 claude を起動する**（検証目的でも）: `CLAUDE_CODE_*` 環境変数を継承して
    記録が残らない。検証は必ず Task Scheduler 経由（`Start-ScheduledTask`）で行う
- 巡回の内容は `watchlist/report_template_rotation.md`（相場観 v1.0）に従う。
  起動時に渡す指示文は `watchlist/trigger_prompt_v1.md` にある
  （クラウドのトリガーは旧仕様のまま書き換えできず「NBIS売却」等の決着済み指示が残っていた。
  移植時にその負債は捨て、`trigger_prompt_v1.md` の文面から始める）
- 日次=差分（読み切り1分）／土曜=注目銘柄の深掘り／月初=候補宇宙のゼロベース再構築
- 走らなかった日があっても `watchlist/rotation_state.md` が前回の座標を保持しているので、
  次回に差分としてまとめて扱える

## 5. ファイル地図

| 用途 | ファイル |
|---|---|
| 恒久ルール（自動ロード） | `CLAUDE.md` |
| 現在地・保有・未解決 | `STATUS.md` 冒頭ブロック |
| 保有と予算の機械可読ソース | `watchlist/holdings.json` |
| 相場観の差分基準（毎回読む） | `watchlist/rotation_state.md` |
| 仮説と反証条件（毎回読む） | `watchlist/thesis_register.md` |
| レポート仕様 | `watchlist/report_template_rotation.md` |
| ローテの機械判定 | `scripts/rotation_check.mjs`／計器は`watchlist/.rotation-tickers` |
| 保有・仮説の機械判定 | `scripts/patrol_check.mjs` |
| 保有外スキャン | `scripts/scan_rank.mjs`／候補は`watchlist/.scan-tickers` |
| 株価取得 | `scripts/fetch_prices.mjs` |
| 日次3年の株価（決算日前後の分析用） | `watchlist/daily_long.json` |
| 決算翌営業日の反応を計算 | `scripts/earnings_reaction.mjs`／日付は`watchlist/earnings_dates.json` |
| 3倍可否の判断（電力4社・MU） | `watchlist/handan_denryoku_2026-09-08.md` |
| データ取得の制約 | `research/data_source_limits.md` |
| トリガー差し替え用文面 | `watchlist/trigger_prompt_v1.md` |
| 過去の判断 | `watchlist/` の日付つきファイル |
| 過去の調査アーカイブ | `STATUS_archive.md`、`research/` |
| 退役（履歴として保存） | `watchlist/banmen_state.md`、`report_template_banmen.md` |

---

## 6. 今の相場観（2026-09-04終値時点。差分の基準は `watchlist/rotation_state.md`）

- **象限: 逆金融相場**（景気-1／金利+2）。2026-08-31に業績相場から移動し、9/4まで5日連続
- **主役: エネルギー。教科書と一致5日連続**（20日+11.4%）。**資本財が11位＝最下位（-5.4%）**
- **金利の局面**: 10年金利4.78%（52週+17.7%、上昇トレンド）。
  **インフレの正体は供給側**——関税が2%超過分の約半分（セントルイス連銀・SF連銀）、
  原油52週+45.5%（米イラン戦争でホルムズ通航が日100隻超→約5隻）。
  賃金は前年比+3.1%で、2%目標と整合的とされる目安（生産性込み約3.5%）を**下回る**。
  → **利上げは「インフレが落ちるまで」ではなく「戦争が終わるまで」続きうる。**
  EIAは中東産油の平時回帰が**2027年初頭**までずれ込むと予測。
- **★景気軸が割れている（2026-09-04〜）**: 価格は「景気は弱い」、ハードデータは「弱くない」。
  8月雇用統計は**+16.2万人**（予想約5.5万）、6月7月も合計+5.5万の上方修正で
  **「7月は雇用が減った」という事実自体が消えた**。**次の巡回で業績相場側へ移る可能性がある。**
- **決定要因はCPIに移った**: ウォラー理事が9/3に
  「8月CPIでディスインフレ継続が確認されれば9月据え置きを支持」と条件を明示。
- **大局（週足）**: **20日窓は基準日の入れ替わりで符号が反転するので、趨勢は必ず週足で見ること**
  （原油の20日変化が2日で+3.0%→-3.1%と反転した実例あり）
- **★保有5銘柄は全部AIデータセンターの1テーマ**（メモリMU・ストレージ制御SIMO・電力POWL・
  冷却MOD・DC IREN）。2026-09-04はDellのAIサーバー受注残$950億という**ニュース1本で総資産が+6.2%**動いた。
  同日S&P500セクターは公益のみプラス。**分散しているように見えて実質1銘柄。**

### ★ 2026-09-08の決定: 待ちへ一本化（`watchlist/handan_denryoku_2026-09-08.md`）

**逆金融相場で3倍が狙える候補は無い。** 候補42銘柄＋電力4社を検証した結論。

- **MUを却下**: 時価総額$1.15兆（株数1,129,393,151の一次確認済み）。3倍＝$3.44兆。
  **現在のPERは約8.5倍**＝市場が「この利益は続かない」と値付けしている循環株の典型。
  粗利率84.9%は産業の過去ピーク（55〜60%）を大きく超える異常値。既に約10倍動いた後。
- **電力4社（VST・TLN・CEG・GEV）を却下**: **PJM容量市場が3回連続で政治的上限に張り付き**
  （$325〜333/MW日、上限が無ければ約$500）。PJMは2029/30まで延長申請済み。
  **上値が行政的に封じられている＝構造的で、待っても直らない。**
  さらにVSTは2026年約100%・2027年約94%ヘッジ済みで、卸価格が+75%上がっても取れない。
  GEV($251B)・CEG($107B)は3倍後の規模が非現実的。
- **決めた3点**: ①逆金融相場では3倍を狙わない ②**投下条件④の時限「2026年12月末」を外すか延ばす**
  （金利ピークは2027年の見立てで、時限は誤った象限での執行を強制する装置になる）
  ③**待つ期間の仕事は「金融相場用の`.scan-tickers`」を作ること**（現在のリストは逆金融相場用で使えない）
- 待つコストはほぼゼロ: 総リスク資本$18,599、3倍目標$55,796。政策金利3.50〜3.75%で15ヶ月待つと
  約$19,440となり、**必要倍率は3.00倍→2.87倍**。倍率が圧縮される象限から拡大する象限へ場を移せる。

---

## 7. 未解決の宿題（クラウドでは解けなかった）

1. **IREN Q4売上のコンセンサス**が$132M説と$157.1M説で食い違い、決着していない
2. **MODのGoogle $4B冷却契約** — 会社は顧客名非開示。Hunterbrook Mediaの流出資料報道のみ
3. **Apple対米上院の中国メモリ回答**（8/21期限）— 公式回答が確認できないまま
4. **IRENの減損$450.4Mの一次確認** — マイニング設備の廃止によるものと二次ソースは言うが未検証
5. **IRENの転換社債の条件**（転換価格・キャップドコール）の一次確認
6. **IRENのATM枠$6Bが実際にどれだけ執行されたか** — 発行済株式数の推移で希薄化を実測する。
   基準は394,058,648株（FY26の10-K表紙）。次の四半期報告で取る
7. **MUの現金・営業CFの絶対額** — 出所により桁が食い違い、未検証のまま

8. **2026-09-08の電力4社調査は5本すべて一次資料に到達できていない。**
   SEC EDGAR・各社IR・主要金融メディアが全てegressプロキシで遮断され、
   **エージェントが「一次」と表記した数字も実際は報道の要約。**
   取り直すべきもの: VST/TLN/CEGの発行済株式数と2027年末までの満期額、CEGの契約比率の具体値、
   MUの四半期実績（MUの株数のみ10-Q表紙の値を取得済み）
9. **金融相場用の`.scan-tickers`をゼロベースで作る**（2026-09-08の決定③）。
   現在のリストは逆金融相場用。象限の移動は速く、移ってから探すのでは間に合わない

**1〜9はすべてSEC提出書類か会社IRを直接読めば解ける。ローカルの最初の仕事にすること。**

**解決済み（2026-09-05）**: IRENのBlue Owl $24億の性格。
Blue Owl公式リリース（8/28）とIRENの8-Kで確認し、**株式ではなく金利9%・満期2.5年の担保付債務**と判明。
→ クラウドからでも会社IRのプレスリリース経由なら取れる場合がある。SECが403でも諦めずIRを当たること。

---

## 8. 過去に起きた事故（同じ失敗を繰り返さないため）

| 事故 | 原因 | 対策 |
|---|---|---|
| IRENの株数を37M株誤り（357M→394M） | 記事の孫引きを「一次資料」と称した | SECから機械で取る |
| 希薄化を年率14%と誤り（実際45.7%） | 同上 | 同上 |
| 仮説Cが3/5まで進行したのを3日見落とし | 反証条件を`node -e`で手打ちし代理指標にすり替わった | `patrol_check.mjs`に条件を写す。手打ち禁止 |
| 良好な株価データを空データで上書き | `fetch_prices.mjs`をローカル実行し403で全滅 | 全滅時は上書きしないガードを実装済み |
| POWL/MODの取得単価が誤り | 口座別ロットを1つの平均で記録していた | `holdings.json`にロット単位で保存 |
| 「原油高→金利上昇の圧力が消えた」と誤報 | 20日窓の基準日入れ替わりを趨勢と誤認 | 週足の大局層を追加済み |
| IRENを降ろす理由を「ATM枠$6Bで希薄化」と誤記 | 実際の資金調達は債務だった。一次確認前に機構を断定した | 会社IRで裏を取ってから機構を書く（結論は変わらなかった） |
| 利上げ確率の出所差を「乖離」と誤読 | 8/31・9/3・9/4のスナップショットを同時点として比較した | **確率・織り込みは必ず取得日とセットで扱う** |

**共通する構造**: **確認していないことを「確認した」と書いた。** 出所と日付のない数字を出さない。

---

## 9. ユーザーとの約束・作法

- 結論・現在地を先に述べる
- **方向を語る主張には日付つきの2点を必ず添える**（「下げている」は無効、「7/29の$29.31から8/5の$38.89へ+33%」なら有効）
- 株価には必ず`quoteTime`を添える
- 保有は軌跡で書く（`取得値（取得日）→現値`）。含み損益率は月初のみ
- **「わからない」「まだ調べていない」は推奨の後の注釈ではなく、推奨の前に言う**
- 分析は完成するまで出さない。途中経過で相談しない
- プロセスの決定をユーザーに承認させない（手順・ツール設定はこちらが決める）
- **保守的な分散投資助言に薄めない**
